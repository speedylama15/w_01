import { Plugin } from "@tiptap/pm/state";
import { Decoration, DecorationSet } from "@tiptap/pm/view";

import { MultiBlockSelection } from "../selections/MultiBlockSelection";

export const DisplayMultiBlockSelection = new Plugin({
  props: {},
});
